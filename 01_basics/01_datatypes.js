"use strict"; // treats all the js code as the latest version.

let stringData = "Krishna";
console.log(typeof stringData);

let numData = 232;
console.log(typeof numData);

console.log("type of null is" + typeof null);
console.log("type of undefined is"+ typeof undefined)

// Types of datatype:

// Number
// BigInt
// String
// Boolean
// Null
// Undefined
// symbol